@model SpaDay.ViewModels.AddUserViewModel
@*@using SpaDay.ViewModels;
    @model AddUserViewModel*@

<h1>Sign Up Form</h1>

@*@if (ViewBag.error != null)
    {
        <p>@ViewBag.error</p>
    }*@

@*First iteration -----------------------------*@

@*<form method="post" action="/user">
        Username:
        <br>
        @if (ViewBag.error == null)
        {
            <input type="text" name="username" />
        }
        else
        {
            <input type="text" name="username" value="@ViewBag.userName" />
        }
        <br>
        Email:
        <br>
        @if (ViewBag.error == null)
        {
            <input type="email" name="email" />
        }
        else
        {
            <input type="email" name="email" value="@ViewBag.eMail" />
        }
        <br>
        Password:
        <input type="password" name="password" />
        <br>
        Confirm Password:
        <br>
        <input type="password" name="verify" />
        <br>
        <input type='submit' value='Submit'>
    </form>*@

@*Second iteration -----------------------------*@

@*<div class="form-group">
        <form method="post" action="/user">

            Username:
            <br>
            @if (ViewBag.error == null)
            {
                <label asp-for="Username"></label>
                <input asp-for="Username" />
                <span asp-validation-for="Username"></span>
            }
            else
            {
                <input type="text" name="username" value="@Model.Username" />
            }
            <br>
            Email:
            <br>
            @if (ViewBag.error == null)
            {
                <label asp-for="Email"></label>
                <input type="email" asp-for="Email" />
                <span asp-validation-for="Email"></span>
            }
            else
            {
                <input type="email" asp-for="Email" value="@Model.Email" />
            }
            <br>
            Password:
            <label asp-for="Password"></label>
            <input type="password" asp-for="Password" />
            <span asp-validation-for="Password"></span>
            <br>
            Confirm Password:
            <br>
            <label asp-for="VerifyPassword"></label>
            <input type="password" asp-for="VerifyPassword" />
            <span asp-validation-for="VerifyPassword"></span>
            <br>
            <input type='submit' value='Submit'>
        </form>
    </div>*@


@*Third iteration -----------------------------*@

<div class="form-group">
    <form method="post" action="/user">
        <label asp-for="Username">Please choose your username</label>
        <input asp-for="Username" value="@Model.Username" />
        <span asp-validation-for="Username"></span>
        <br>

        <label asp-for="Email"></label>
        <input type="email" asp-for="Email" value="@Model.Email" />
        <span asp-validation-for="Email"></span>
        <br>

        <label asp-for="Password"></label>
        <input type="password" asp-for="Password" />
        <span asp-validation-for="Password"></span>
        <br>

        <label asp-for="VerifyPassword">Please Re-Type your password</label>
        <input type="password" asp-for="VerifyPassword" />
        <span asp-validation-for="VerifyPassword"></span>
        <br>
        <input type='submit' value='Submit'>
    </form>
</div>


